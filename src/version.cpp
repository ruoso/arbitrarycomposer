namespace arbc {

// Anchor translation unit for the umbrella library; the real version API
// arrives with install/packaging (doc 10).
const char* version_string() { return "0.1.0"; }

} // namespace arbc
